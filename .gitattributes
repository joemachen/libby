# Normalize line endings: LF in the repo, native checkout on each OS
* text=auto

# Always LF for shell scripts and CI configs (run on Linux runners)
*.sh   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# Always CRLF for Windows batch files
*.bat  text eol=crlf
